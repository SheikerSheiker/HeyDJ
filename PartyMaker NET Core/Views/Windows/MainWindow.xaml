<Window x:Class="PartyMaker_NET_Core.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PartyMaker_NET_Core.ViewModels"
        xmlns:l="clr-namespace:PartyMaker_NET_Core"
        Title="PartyMaker"
        Height="450"
        Width="800">
    <Window.DataContext>
        <vm:SimpleWindowViewModel />
    </Window.DataContext>
    <Grid Background="#FFF1D8A2">
        <DockPanel Height="50"
                   VerticalAlignment="Top">
            <Button Style="{StaticResource OpenBtnStyle}"
                    DockPanel.Dock="Left">
                abaldjfla
            </Button>
            <Button Style="{StaticResource OpenBtnStyle}"
                    DockPanel.Dock="Left">
                FGH
            </Button>
            <Button Style="{StaticResource OpenBtnStyle}" DockPanel.Dock="Right">
                Svravka
            </Button>
            <TextBlock Text="ПатиМейкер"
                       HorizontalAlignment="Center"
                       FontSize="48"
                       Foreground="#FFFF7A00"
                       FontWeight="Bold"
                       FontFamily="Calibri" />
        </DockPanel>
    </Grid>
</Window>
